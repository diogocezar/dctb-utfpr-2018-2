/* Exemplo de erro de divisão por zero (Erro de execução)
   O programador deve evitar que isto ocorra             */

#include<stdio.h>

int main(){
	printf("A divisao vale: %d",10/0);
	return 0;
}
